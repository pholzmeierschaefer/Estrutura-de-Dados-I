/*

stdlib.h
        malloc
        free
    
1. Alocar memória
2. atribuir valores para o espaço alocado e null
3. encadiar
//
    p = (INT *)malloc(sizeof(INT));  // aloca memória

        p->value = i;                //
        p->next = NULL;              // atribui valores para o espaço alocado e null



    if (first == NULL)
        {
            first = p;
            aux = p;
        }                                  //encdiamento
        else
        {
            aux->next = p;
            aux = p;
        }




    for(aux=first; aux != NULL; aux = aux->next)   //percorrer a lista

    
    


*/

